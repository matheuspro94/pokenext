import styled from "styled-components"

const About = styled.div`
  text-align: center;

  h1 {
    font-size: 2rem;
    margin-bottom: 1rem;
  }

  p {
    margin: 0 auto 1.5rem;
    max-width: 60%;
    line-height: 1.4;
  }
`
export default About;